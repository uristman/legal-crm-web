# 🆕 НОВАЯ ФУНКЦИОНАЛЬНОСТЬ: Привязка клиентов к календарю

## ✅ Что добавлено:

### 1. **Модальное окно события**
- Добавлено поле **"Клиент"** - выпадающий список
- Добавлено поле **"Дело"** - выпадающий список, связанный с выбранным клиентом
- Автоматическая загрузка дел при выборе клиента

### 2. **API функциональность**
- ✅ `GET /api/events/{id}` - получение конкретного события с клиентом
- ✅ `GET /api/cases?client_id=ID` - фильтрация дел по клиенту
- ✅ `POST /api/events` - создание события с клиентом и делом
- ✅ `PUT /api/events/{id}` - редактирование события с клиентом

### 3. **Интерфейс**
- ✅ Кнопка **"Редактировать"** в таблице событий
- ✅ Отображение имени клиента в таблице событий
- ✅ Автоматическое обновление списка дел при выборе клиента

---

## 🎯 Как использовать:

### **Создание события с клиентом:**
1. Откройте вкладку "Календарь"
2. Нажмите "Добавить событие"
3. Выберите клиента из списка
4. При выборе клиента автоматически загрузятся его дела
5. Выберите дело (опционально)
6. Заполните остальные поля и сохраните

### **Редактирование события:**
1. В таблице событий нажмите кнопку "Редактировать"
2. Измените клиента или дело
3. Сохраните изменения

---

## 🧪 Тестирование:

### Тест API:
```bash
# Создание события с клиентом
curl -X POST http://localhost:5000/api/events \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Встреча с клиентом",
    "event_type": "Встреча с клиентом",
    "event_date": "2025-11-25",
    "client_id": 3
  }'

# Получение события с клиентом
curl http://localhost:5000/api/events/1

# Фильтрация дел по клиенту
curl "http://localhost:5000/api/cases?client_id=3"
```

### Тест веб-интерфейса:
1. Откройте http://localhost:5000
2. Перейдите на вкладку "Календарь"
3. Нажмите "Добавить событие"
4. Убедитесь что:
   - ✅ Есть поле "Клиент" с списком клиентов
   - ✅ При выборе клиента загружаются его дела
   - ✅ Событие сохраняется с привязкой к клиенту
   - ✅ В таблице отображается имя клиента

---

## 🔧 Технические детали:

### Изменения в коде:

#### HTML (templates/index.html):
```html
<!-- Добавлены поля выбора клиента и дела -->
<div class="col-md-6 mb-3">
    <label class="form-label">Клиент</label>
    <select class="form-select" id="eventClient">
        <option value="">Выберите клиента</option>
    </select>
</div>
<div class="col-md-6 mb-3">
    <label class="form-label">Дело</label>
    <select class="form-select" id="eventCase">
        <option value="">Выберите дело</option>
    </select>
</div>
```

#### JavaScript (static/js/main.js):
```javascript
// Загрузка клиентов в модальное окно
function loadClientsForEvent()

// Загрузка дел при выборе клиента
function loadCasesForClient(clientId)

// Загрузка данных события для редактирования
function loadEventData(eventId)

// Обновленная функция сохранения
function saveEvent() {
    const formData = {
        // ... существующие поля ...
        client_id: $('#eventClient').val() || null,
        case_id: $('#eventCase').val() || null
    };
}
```

#### Backend (app.py):
```python
# Новый эндпоинт для получения события по ID
@app.route('/api/events/<int:event_id>', methods=['GET'])
def get_event(event_id):

# Обновленный эндпоинт дел с фильтрацией
@app.route('/api/cases', methods=['GET'])
def get_cases():
    client_id = request.args.get('client_id')
    # SQL запрос с WHERE для фильтрации по клиенту
```

---

## 🎉 Результат:

✅ **Полная интеграция клиентов в календарь**  
✅ **Автоматическая фильтрация дел по клиенту**  
✅ **Интуитивный интерфейс выбора**  
✅ **Редактирование существующих событий**  
✅ **Корректное отображение в таблице**  

**🚀 Legal CRM Web теперь полностью централизован вокруг клиентов!**