# 🚀 Legal CRM Web - Облачное развертывание

## 📋 Подготовка к развертыванию

### ✅ Файлы для развертывания созданы:
- `Procfile` - Конфигурация для Heroku-совместимых платформ
- `runtime.txt` - Версия Python для облака
- `requirements.txt` - Обновленные зависимости с gunicorn
- `app.py` - Адаптирован для production окружения

---

## 🌟 Вариант 1: Render.com (Рекомендуется - БЕСПЛАТНО)

### Шаг 1: Подготовка репозитория
1. Создать аккаунт на https://render.com
2. Создать репозиторий на GitHub с файлами из папки `legal_crm_web`
3. Включить в репозиторий все файлы:
   ```
   app.py
   requirements.txt
   Procfile
   runtime.txt
   templates/index.html
   static/css/main.css
   static/js/main.js
   ```

### Шаг 2: Развертывание на Render
1. Войти в Render.com → Dashboard → "New +" → "Web Service"
2. Подключить GitHub репозиторий
3. Настроить параметры:
   - **Name**: `legal-crm` (или любое название)
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
   - **Region**: Выбрать ближайший к вам

### Шаг 3: Настройка переменных окружения
В Render Dashboard добавить Environment Variables:
- `DEBUG`: `False`
- `SECRET_KEY`: `your-unique-secret-key-here`
- `DATABASE_NAME`: `legal_crm.db`

### Шаг 4: Первый запуск
1. Нажать "Create Web Service"
2. Дождаться сборки (2-3 минуты)
3. Получить URL вида: `https://legal-crm.onrender.com`
4. Добавить демо-данные: `curl -X POST https://ваш-URL/api/demo-data`

---

## 🌟 Вариант 2: Heroku.com (Популярный)

### Шаг 1: Установка Heroku CLI
```bash
# Windows - скачать с https://devcenter.heroku.com/articles/heroku-cli
# macOS: brew tap heroku/brew && brew install heroku
# Linux: curl https://cli-assets.heroku.com/install.sh | sh
```

### Шаг 2: Авторизация и деплой
```bash
# Перейти в папку с проектом
cd legal_crm_web

# Авторизоваться
heroku login

# Создать приложение
heroku create legal-crm-ваше-имя

# Загрузить на Heroku
git init
git add .
git commit -m "Legal CRM Web App"
git push heroku main

# Запустить
heroku ps:scale web=1
```

### Шаг 3: Настройка переменных
```bash
# Установить переменные окружения
heroku config:set DEBUG=False
heroku config:set SECRET_KEY=your-secret-key
```

---

## 🌟 Вариант 3: Railway.app (Современный)

### Шаг 1: Создание проекта
1. Регистрация на https://railway.app
2. "New Project" → "Deploy from GitHub repo"
3. Выбрать репозиторий с приложением

### Шаг 2: Автоматическое развертывание
Railway автоматически определит Python и развернет приложение

### Шаг 3: Настройки
- В Dashboard найти переменную `PORT`
- Если нужно - добавить Environment Variables

---

## 🌟 Вариант 4: PythonAnywhere (Для Python-приложений)

### Шаг 1: Регистрация
1. https://www.pythonanywhere.com/
2. Создать аккаунт (есть бесплатный план)

### Шаг 2: Загрузка файлов
1. Открыть File Manager
2. Загрузить все файлы в домашнюю папку
3. Создать виртуальную среду: `mkvirtualenv legal_crm`

### Шаг 3: Настройка веб-сервера
1. Web → Add a new web app
2. Manual configuration
3. Указать WSGI configuration file
4. Добавить путь к приложению

---

## 🔧 Настройка для production

### Безопасность
```python
# В app.py уже добавлено:
app.config['SECRET_KEY'] = os.environ.get('SECRET_KEY')
app.config['DEBUG'] = os.environ.get('DEBUG', 'False') == 'True'
```

### База данных
- SQLite файл сохраняется в облаке
- Для больших объемов данных рекомендуется PostgreSQL

### Мониторинг
- Render/Heroku предоставляют логи
- Мониторинг аптайма включен автоматически

---

## 🎯 Быстрый старт (Render.com)

1. **Создать GitHub репозиторий**
2. **Загрузить все файлы из `legal_crm_web`**
3. **Подключить к Render.com**
4. **Получить URL и начать использовать!**

---

## 📞 Поддержка

Если возникнут проблемы с развертыванием:
1. Проверить логи на платформе
2. Убедиться что все файлы загружены
3. Проверить переменные окружения

---

## 🔄 Обновление приложения

После изменений в коде:
1. Загрузить изменения в GitHub
2. Платформа автоматически пересоберет приложение
3. Новая версия станет доступна через несколько минут