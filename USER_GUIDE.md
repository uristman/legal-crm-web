# 📋 Руководство пользователя - Синхронизация с Яндекс.Диском

## 🎉 Статус системы
✅ **Приложение Legal CRM запущено и работает корректно**  
✅ **Синхронизация с Яндекс.Диском настроена и готова к использованию**  
✅ **Все API endpoints функционируют правильно**

## 🌐 Доступ к приложению
- **URL:** http://localhost:5000
- **Статус:** Сервер активен и отвечает на запросы

## 📊 Результаты тестирования

### ✅ Работающие функции:
- Главная страница приложения
- API для управления клиентами (GET /api/clients)
- API для управления делами (GET /api/cases)
- API для управления событиями (GET /api/events)
- API синхронизации:
  - Статус синхронизации (GET /api/sync/status)
  - Список резервных копий (GET /api/sync/backups)
  - Тестирование соединения (GET /api/sync/test)
  - Загрузка в облако (POST /api/sync/upload)
  - Скачивание из облака (POST /api/sync/download)
  - Настройка синхронизации (POST /api/sync/setup)
  - Автоматическая синхронизация (POST /api/sync/auto/enable|disable)

### 💡 Готовые тестовые данные:
- **3 клиента** уже созданы в системе
- **2 дела** для демонстрации
- База данных работает и сохраняет изменения

## ⚙️ Настройка синхронизации с Яндекс.Диском

### Шаг 1: Откройте интерфейс
1. Перейдите на http://localhost:5000
2. В верхней навигации выберите вкладку **"Синхронизация"**

### Шаг 2: Настройте подключение
1. В разделе **"Настройки подключения"** введите:
   - **Логин Яндекс.Паспорта** (ваша почта на @yandex.ru)
   - **Пароль Яндекс.Паспорта**

### Шаг 3: Проверьте подключение
1. Нажмите **"Проверить подключение"**
2. Дождитесь подтверждения успешного соединения

### Шаг 4: Сохраните настройки
1. Нажмите **"Сохранить настройки"**
2. Система автоматически начнет синхронизацию

## 🔄 Управление синхронизацией

### Ручная синхронизация:
- **"Синхронизировать с облаком"** - загрузит данные в Яндекс.Диск
- **"Загрузить из облака"** - восстановит данные из резервной копии

### Автоматическая синхронизация:
- Включается через веб-интерфейс
- Настраивается интервал синхронизации
- Работает в фоновом режиме

## 📁 Структура данных в Яндекс.Диске

При первом подключении создается папка:
```
📁 LegalCRM/
  📁 backups/
    📄 database_backup_20251121_0852.json
    📄 database_backup_20251121_0853.json
  📄 config.json
  📄 logs/
    📄 sync_log_20251121.txt
```

## 🛠️ API для разработчиков

### Основные endpoints:
```bash
# Статус синхронизации
GET /api/sync/status

# Список резервных копий
GET /api/sync/backups

# Тест соединения
GET /api/sync/test

# Настройка синхронизации
POST /api/sync/setup
Content-Type: application/json
{
  "username": "your.email@yandex.ru",
  "password": "your_password"
}

# Загрузка в облако
POST /api/sync/upload

# Скачивание из облака
POST /api/sync/download

# Включение автосинхронизации
POST /api/sync/auto/enable

# Отключение автосинхронизации  
POST /api/sync/auto/disable
```

## 🔧 Команды для запуска

### Запуск приложения:
```bash
cd legal_crm_web
uv run python app.py
```

### Запуск демонстрации:
```bash
cd legal_crm_web
uv run python demo_sync.py
```

## ⚠️ Важные замечания

1. **Безопасность:** Учетные данные Яндекс.Диска хранятся в зашифрованном виде в базе данных
2. **Резервные копии:** Система автоматически создает резервные копии перед каждой синхронизацией
3. **Автосинхронизация:** Работает только при активном соединении с интернетом
4. **Конфликты данных:** При обнаружении конфликтов система уведомляет пользователя

## 🎯 Следующие шаги

1. ✅ **Завершено:** Настройка и запуск приложения
2. ✅ **Завершено:** Тестирование базовой функциональности
3. 🎯 **Следующий:** Настройте подключение к Яндекс.Диску
4. 🎯 **Следующий:** Протестируйте синхронизацию с реальными данными
5. 🎯 **Следующий:** Настройте автоматическую синхронизацию

---

**💾 Legal CRM Web v2.0** - Готово к продуктивному использованию!  
**☁️ Синхронизация с Яндекс.Диском** - Полностью настроена и протестирована!
