# ☁️ Render.com - Частые вопросы и настройки

## 🤔 Нужно ли настраивать переменные окружения на Render.com?

### ✅ **Короткий ответ: НЕТ, можно развернуть сразу без настроек!**

Наше приложение Legal CRM Web уже настроено для облачного развертывания и не требует обязательных переменных окружения.

---

## 🚀 Развертывание БЕЗ переменных окружения

### Что происходит автоматически:
- **Порт**: Берется из переменной `PORT` Render автоматически
- **База данных**: Создается файл `legal_crm.db` в папке приложения
- **Отладка**: Автоматически отключается в продакшн

### Минимальная настройка Render:
1. **Build Command**: `pip install -r requirements.txt`
2. **Start Command**: `gunicorn app:app`
3. Все остальное - автоматически!

---

## 🔧 Дополнительные настройки (опционально)

### Если хотите добавить переменные окружения:

#### Environment → Variables:
```
DEBUG: False
SECRET_KEY: ваш-уникальный-ключ-здесь
DATABASE_NAME: legal_crm.db
```

### Где взять SECRET_KEY:
```bash
# В терминале выполните:
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## 🎯 Рекомендуемый план развертывания

### Этап 1: Быстрое развертывание (БЕЗ переменных)
1. ✅ Подключить GitHub репозиторий
2. ✅ Build Command: `pip install -r requirements.txt`
3. ✅ Start Command: `gunicorn app:app`
4. ✅ Нажать "Create Web Service"
5. ✅ Дождаться развертывания (2-3 минуты)
6. ✅ Протестировать приложение

### Этап 2: Настройка безопасности (потом)
1. Добавить переменные окружения в Render Dashboard
2. Перезапустить сервис
3. Проверить что все работает

---

## 📊 Настройки Render.com - пошагово

### При создании Web Service:

#### Основные настройки:
- **Name**: `legal-crm` (или любое имя)
- **Region**: выберите ближайший к вам
- **Branch**: `main`
- **Root Directory**: оставьте пустым

#### Build and Deploy:
- **Runtime**: Python 3
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `gunicorn app:app`

#### Environment:
- **Environment Type**: Production (по умолчанию)

### Переменные окружения (если хотите добавить):
Environment → Variables:
```
DEBUG = False
SECRET_KEY = [сгенерированный ключ]
DATABASE_NAME = legal_crm.db
```

---

## 🔍 Проверка работы приложения

### После развертывания:

#### 1. Проверьте главную страницу:
- Откройте URL из Render Dashboard
- Должен загрузиться интерфейс Legal CRM

#### 2. Проверьте API:
```bash
curl https://ваш-url-render.com/api/stats
```
Должен вернуть JSON с статистикой

#### 3. Создайте демо-данные:
```bash
curl -X POST https://ваш-url-render.com/api/demo-data
```

#### 4. Проверьте работу веб-интерфейса:
- Добавьте клиента
- Создайте дело
- Добавьте услугу

---

## 🚨 Типичные ошибки и решения

### Ошибка "Application failed to start":
**Причина**: Проблема в Build или Start команде

**Решение**:
1. Проверьте логи в Render Dashboard
2. Убедитесь что Build Command: `pip install -r requirements.txt`
3. Убедитесь что Start Command: `gunicorn app:app`

### Ошибка "Module not found":
**Причина**: Flask не установлен

**Решение**:
1. Проверьте содержимое `requirements.txt`
2. Должно содержать:
   ```
   Flask==3.0.0
   Flask-CORS==4.0.0
   Werkzeug==3.0.1
   gunicorn==21.2.0
   ```

### Приложение запускается, но нет данных:
**Причина**: Не созданы демо-данные

**Решение**:
```bash
curl -X POST https://ваш-url-render.com/api/demo-data
```

### Ошибка 500 Internal Server Error:
**Причина**: Проблемы с базой данных

**Решение**:
1. Проверьте логи в Render Dashboard
2. Проверьте что файл `legal_crm.db` создается
3. Перезапустите сервис в Render Dashboard

---

## 💡 Полезные советы

### Для разработки:
- Используйте `DEBUG=True` локально
- В облаке всегда `DEBUG=False`

### Для безопасности:
- Генерируйте уникальный SECRET_KEY
- Не храните секретные ключи в коде

### Для производительности:
- Gunicorn сам оптимизирует работу
- SQLite подходит для небольших нагрузок

---

## 🎉 После успешного развертывания

### Что вы получите:
- ✅ Публичный URL (например: `https://legal-crm.onrender.com`)
- ✅ HTTPS соединение
- ✅ Автоматический мониторинг
- ✅ Логи в реальном времени
- ✅ Автоматические обновления при push в GitHub

### Регулярное обновление:
```bash
# Внесли изменения в код?
git add .
git commit -m "Обновление описания"
git push origin main
# Render автоматически пересоберет и развернет!
```

---

**🎯 Render.com готов к работе из коробки - просто подключите GitHub репозиторий и нажмите Deploy!**